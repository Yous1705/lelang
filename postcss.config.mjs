const config = {
  // For Tailwind v4 the PostCSS plugin has moved to @tailwindcss/postcss.
  // Use that plugin here together with autoprefixer.
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};

export default config;
